{
  "name": "news_summaries_backend",
  "version": "1.0.0",
  "description": "news summaries API",
  "main": "index.js",
  "type": "module",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "babel src -d dist --source-maps",
    "serve": "SET NODE_ENV=production && node dist/index.js",
    "start": "SET NODE_ENV=development && babel-node src/index.js",
    "dev": "SET DEBUG=server:debug NODE_ENV=development && nodemon src/index.js --exec babel-node",
    "test": "SET DEBUG=server:debug NODE_ENV=test && mocha --require babel-core/register --reporter spec --exit   tests/ --exec babel-node",
    "test:watch": "SET DEBUG=server:debug NODE_ENV=development mocha  --require babel-core/register --watch --reporter  spec   tests/ --exec babel-node",
    "test_jest": "jest",
    "migration-dev": "SET DEBUG=server:debug NODE_ENV=development && ",
    "eslint": "eslint src/**/*.js --ignore-pattern \"node_modules/\""
  },
  "keywords": [
    "news",
    "summaries",
    "summary",
    "podcast"
  ],
  "author": "Jose Contreras",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-node8": "^1.2.0",
    "config": "^3.3.2",
    "debug": "^4.2.0",
    "jest": "^26.6.1",
    "nodemon": "^2.0.5",
    "supertest": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "migrate-mongo": "^8.1.4",
    "mongoose": "^5.10.9",
    "scrape-it": "^5.2.4"
  },
  "babel": {
    "presets": [
      "node8"
    ]
  },
  "engines": {
    "node": ">=8"
  },
  "eslintConfig": {
    "plugins": [
      "node"
    ],
    "extends": [
      "eslint:recommended",
      "plugin:node/recommended"
    ],
    "rules": {
      "node/no-unsupported-features/es-syntax": 0,
      "node/no-unsupported-features/es-builtins": 0
    },
    "env": {
      "node": true,
      "es6": true
    },
    "parserOptions": {
      "sourceType": "module",
      "ecmaVersion": 2018
    },
    "jest" : {
      "testEnvironment": "node",
      "coveragePathIgnorePatterns": [
        "/node_modules/"
      ]
    }
  }
}
